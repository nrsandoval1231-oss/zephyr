*.mp4 filter=lfs diff=lfs merge=lfs -text
*.mov filter=lfs diff=lfs merge=lfs -text
*.m4v filter=lfs diff=lfs merge=lfs -text

# Built reels stay as ordinary git objects: this build environment has no
# outbound access to lfs.github.com, so LFS uploads from here are refused.
reel/*.mp4 !filter !diff !merge -text
